import { AuthenticateOptions } from "passport";
export interface AuthorizeOptions extends AuthenticateOptions {
    security?: {
        [key: string]: string[];
    };
    originalUrl?: boolean;
}
export declare function Authorize(protocol?: string | string[], options?: AuthorizeOptions): Function;
