import { IProtocolOptions } from "../interfaces/IProtocolOptions";
/**
 * Declare a new Protocol base on a Passport Strategy
 *
 * @decorator
 * @class
 */
export declare function Protocol<T = any>(options: ProtocolOptionsDecorator<T>): any;
export declare type ProtocolOptionsDecorator<T = any> = {
    name: string;
} & Partial<IProtocolOptions<T>>;
