import { BeforeRoutesInit, Configuration, InjectorService, OnReady, PlatformApplication } from "@tsed/common";
import { SwaggerSettings } from "./interfaces";
import { SwaggerService } from "./services/SwaggerService";
/**
 * @ignore
 */
export declare class SwaggerModule implements BeforeRoutesInit, OnReady {
    injector: InjectorService;
    app: PlatformApplication;
    configuration: Configuration;
    swaggerService: SwaggerService;
    private loaded;
    get settings(): SwaggerSettings[];
    /**
     *
     */
    $beforeRoutesInit(): void;
    $onRoutesInit(): void;
    $onReady(): void;
    private getUrls;
    /**
     *
     * @param conf
     * @param urls
     */
    private createRouter;
    private middlewareSwaggerJson;
}
