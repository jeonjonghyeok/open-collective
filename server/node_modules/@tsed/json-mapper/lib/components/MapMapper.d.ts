import { JsonMapperCtx, JsonMapperMethods } from "../interfaces/JsonMapperMethods";
/**
 * Mapper for `Map` type.
 * @jsonmapper
 * @component
 */
export declare class MapMapper implements JsonMapperMethods {
    deserialize<T = any, C = Map<string, T>>(data: {
        [key: string]: any;
    }, ctx: JsonMapperCtx<T, C>): Map<string, T>;
    serialize<T>(data: Map<string, T>, ctx: JsonMapperCtx): any;
}
