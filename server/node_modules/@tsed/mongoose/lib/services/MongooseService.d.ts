import * as Mongoose from "mongoose";
export declare class MongooseService {
    readonly connections: Map<string, Mongoose.Connection>;
    private defaultConnection;
    /**
     *
     * @returns {Promise<"mongoose".Connection>}
     */
    connect(id: string, url: string, connectionOptions: Mongoose.ConnectionOptions, isDefault?: boolean): Promise<any>;
    /**
     *
     * @returns {"mongoose".Connection}
     */
    get(id?: string): Mongoose.Connection | undefined;
    /**
     *
     * @param {string} id
     * @returns {boolean}
     */
    has(id?: string): boolean;
    closeConnections(): Promise<void>;
}
