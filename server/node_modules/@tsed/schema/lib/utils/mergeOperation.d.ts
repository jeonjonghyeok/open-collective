import { SpecTypes } from "../domain/SpecTypes";
/**
 * @ignore
 */
export interface MergeOperationOptions {
    rootPath: string;
    specType: SpecTypes;
    operationId: (path: string) => string | undefined;
    defaultTags: string[];
    tags: string[];
    path: string;
    method: string;
}
/**
 * @ignore
 */
export declare function mergeOperation(obj: any, operation: any, { rootPath, specType, operationId, defaultTags, tags, path, method }: MergeOperationOptions): any;
