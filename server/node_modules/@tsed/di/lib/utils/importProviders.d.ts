import { IProvider } from "../interfaces/IProvider";
/**
 * Import providers from given patterns.
 *
 * @param settings
 * @param properties
 */
export declare function importProviders(settings: Partial<TsED.Configuration>, properties?: string[]): Promise<IProvider<any>[]>;
