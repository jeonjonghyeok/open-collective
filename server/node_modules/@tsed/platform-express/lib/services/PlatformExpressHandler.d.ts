import { HandlerMetadata, OnRequestOptions, PlatformHandler } from "@tsed/common";
/**
 * @platform
 * @express
 */
export declare class PlatformExpressHandler extends PlatformHandler {
    protected createRawHandler(metadata: HandlerMetadata): Function;
    protected onCtxRequest(requestOptions: OnRequestOptions): Promise<any>;
}
