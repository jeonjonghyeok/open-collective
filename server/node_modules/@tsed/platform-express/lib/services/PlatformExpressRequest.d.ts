import { PlatformContext, PlatformRequest } from "@tsed/common";
import type * as Express from "express";
declare module "express" {
    interface Request {
        id: string;
        $ctx: PlatformContext;
    }
}
declare global {
    namespace TsED {
        interface Request extends Express.Request {
            id: string;
            $ctx: PlatformContext;
        }
    }
}
/**
 * @platform
 * @express
 */
export declare class PlatformExpressRequest extends PlatformRequest<Express.Request> {
}
